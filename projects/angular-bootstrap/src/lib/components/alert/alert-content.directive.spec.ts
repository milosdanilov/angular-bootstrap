import { AlertContentDirective } from './alert-content.directive';

describe('AlertContentDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertContentDirective();
    expect(directive).toBeTruthy();
  });
});
